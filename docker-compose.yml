services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: seaglass
      POSTGRES_USER: seaglass
      POSTGRES_PASSWORD: seaglass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
